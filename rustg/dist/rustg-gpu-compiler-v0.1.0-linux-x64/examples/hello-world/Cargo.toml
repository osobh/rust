[package]
name = "hello-world"
version = "0.1.0"
edition = "2021"

# Example project demonstrating basic RustG GPU compiler usage
[package.metadata.rustg]
gpu-target = "sm_110"
cuda-version = "13.0"
prefer-gpu = true